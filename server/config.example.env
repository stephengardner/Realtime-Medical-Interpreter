# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=3001
CLIENT_URL=http://localhost:5173

# Database Configuration (Required)
MONGODB_URI=your_production_mongodb_connection_string_here

# Webhook Configuration (for conversation completion notifications)
WEBHOOK_URL=https://webhook.site/your-webhook-id-here

# Node Environment (development or production)
NODE_ENV=development

# Production Environment Variables (for Heroku deployment)
# PORT will be automatically set by Heroku
# CLIENT_URL should be your production domain (optional in production)
# NODE_ENV should be set to "production" in Heroku
# MONGODB_URI should be your production MongoDB connection string (e.g., MongoDB Atlas) 